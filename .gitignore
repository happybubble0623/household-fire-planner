# Dependencies
node_modules/

# Build output
.next/
out/
build/
dist/
coverage/
next-env.d.ts
tsconfig.tsbuildinfo

# Environment files
.env
.env.*
# ...but keep the committed template of variable names (placeholders only).
!.env.example

# Logs
*.log
npm-debug.log*
yarn-error.log
pnpm-debug.log*
yarn-debug.log*

# Native mobile projects (generated locally via Capacitor).
# The iOS project IS committed (it ships its own ios/.gitignore that excludes
# App/Pods/ and build output) so it travels between Macs. Android stays local.
/android/
.capacitor/

# Deploy / tooling caches
.vercel/
.turbo/

# Local harness/settings
.claude/settings.local.json

# OS cruft
.DS_Store
